# The Vite frontend does not require runtime secrets.
# Backend configuration lives in server/.env.example.
#
# Keep this file as a root-level placeholder for tools that expect .env.example.
# Never commit real keys, tokens, or local .env files.
